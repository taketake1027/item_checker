<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>イベント管理アプリ</title>
  <%= csrf_meta_tags %>
  <%= stylesheet_pack_tag 'application', media: 'all' %> <!-- CSS ファイルを読み込む -->
  <%= javascript_pack_tag 'application' %> <!-- JavaScript ファイルを読み込む -->
</head>
<body>
  <!-- ヘッダー -->
  <%= render 'layouts/header' %>
  
  <main>
    <% if flash[:notice] %>
      <div class="alert alert-success text-center" role="alert">
        <%= t('flash.notice', default: flash[:notice]) %>
      </div>
    <% elsif flash[:alert] %>
      <div class="alert alert-danger text-center" role="alert">
        <%= t('flash.alert', default: flash[:alert]) %>
      </div>
    <% end %>
  <body class="<%= user_signed_in? ? 'logged-in' : 'logged-out' %>">
    <%= yield %> <!-- コンテンツの表示 -->
  </body>
  </main>

  <!-- フッター -->
  <footer>
    <div class="container">
      <p>© 2024 イベント管理アプリ</p>
    </div>
  </footer>
</body>
</html>
